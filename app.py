# legacy module kept for compatibility during the refactor
# all functionality has been moved into the `app` package.
# use `run.py` as the entry point, e.g.:
#
#     python run.py
#     gunicorn run:app
#
# This file is no longer executed by the application and can be removed.

